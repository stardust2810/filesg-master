import { IconButton } from '@filesg/design-system';
import styled from 'styled-components';

export const StyledIconButton = styled(IconButton)`
  background-color: inherit;
`;

export const StyledDropdownContainer = styled.div`
  position: relative;
`;
